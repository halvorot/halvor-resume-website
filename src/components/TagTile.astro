---
interface Props {
  text: string;
  classes?: string;
}

const { text, classes, ...rest } = Astro.props;
---

<span
  class={`border-light-accent/15 bg-light/5 text-light inline-flex rounded-full border px-3 py-1 text-xs ${classes}`}
  {...rest}
>
  {text}
</span>
